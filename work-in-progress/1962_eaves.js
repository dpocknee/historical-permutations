function eaves(n) {
  // first
  let b = Array.from({ length: n + 1 }, (value, index) => index);
  let t = a[x];
  let first = false;
  for (let i = 1; i <= n; i++) {
    b[i] = 0;
  }
  for (let i = n; i >= 2; i--) {
    if(a[i] > t a[i] > a[i-1]) {

    }
  }



}